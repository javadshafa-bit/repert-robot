# پرامپت فاز ۲: رفع ایرادهای بازبینی + سیستم پرداخت و اشتراک + پنل نظارتی سوپرادمین

> **نحوه استفاده:** این فایل را کامل به همان عاملی بده که فاز ۱ (چندمستأجری‌سازی) را روی `repert-robot` پیاده کرد. این سند ادامه‌ی مستقیم آن کار است و فرض می‌کند کد فاز ۱ روی دیسک هست ولی **هنوز روی سرور deploy نشده**.
>
> **ترتیب اجرا مهم است:** اول بخش «الف» (رفع ایرادها) را کامل کن، چون بدون آن deploy فاز ۱ ربات زنده را می‌خواباند. بعد بخش‌های ب و ج.
>
> **قبل از شروع، یک plan کوتاه بنویس و نشانم بده.** بعد مرحله‌به‌مرحله جلو برو و بعد از هر مرحله دستور تأیید بده. هیچ دستوری روی سرور production خودت اجرا نکن.

---

## ۰. تصمیم‌های گرفته‌شده (این‌ها را تغییر نده)

| موضوع | تصمیم |
|---|---|
| درگاه پرداخت | **زرین‌پال (ZarinPal)** |
| مدل فروش | **پرداخت آزاد** — کاربر مبلغ می‌پردازد، دوره‌ی اشتراک محاسبه/تعیین می‌شود |
| تایید دستی سوپرادمین | **حذف می‌شود** — ثبت‌نام → پرداخت → دسترسی فوری، بدون دخالت انسان |
| نقش سوپرادمین | فقط **نظارت** و مدیریت اشتراک؛ حق افزودن/تنظیم ربات ندارد |
| کد تخفیف | سوپرادمین کد و درصد را تعریف می‌کند |
| دوره‌ی اشتراک | سوپرادمین برای هر مستأجر جدا قابل تعیین: از **۱ روز تا نامحدود** |

### ⚠️ یک تناقض که باید حلش کنی (و قبل از پیاده‌سازی از من تأیید بگیری)

«پرداخت آزاد + تعیین دستی دوره» با «دسترسی فوری بعد از پرداخت، بدون دخالت ادمین» در تضاد است: اگر مبلغ آزاد باشد، سیستم از کجا بداند چند روز اشتراک بدهد؟

**راه‌حل پیشنهادی من (پیش‌فرض را همین بگیر مگر خلافش را بگویم):**

- سوپرادمین در تنظیمات پلتفرم یک **«قیمت پایه‌ی هر روز»** (`price_per_day`، به تومان) و یک **«حداقل مبلغ قابل پرداخت»** تعریف می‌کند.
- کاربر در صفحه‌ی پرداخت یا مبلغ دلخواه وارد می‌کند یا تعداد روز را انتخاب می‌کند؛ در هر دو حالت مبلغ نهایی و تعداد روز معادل، **قبل از رفتن به درگاه** به او نشان داده می‌شود.
- بعد از پرداخت موفق: `روز = floor(مبلغ پرداخت‌شده / price_per_day)` و همان‌قدر به `subscription_ends_at` اضافه می‌شود، **بدون دخالت سوپرادمین**.
- سوپرادمین همچنان می‌تواند هر لحظه دوره‌ی هر مستأجر را دستی تغییر دهد (تمدید، کوتاه کردن، یا نامحدود کردن) — این «تعیین دستی دوره» است، نه شرطِ فعال شدن.

اگر این تفسیر را قبول نداری، قبل از نوشتن کد بپرس.

---

# بخش الف — رفع ایرادهای بازبینی فاز ۱

این‌ها نتیجه‌ی یک بازبینی مستقل روی کد فاز ۱ است. سه مورد اول **بازدارنده‌ی deploy** هستند.

## الف-۱ 🔴 پنجره‌ی قطعی ربات بین `migrate` و `refresh-webhook`

مسیر قدیمی `POST /api/bot/webhook` از `routes/web.php` حذف شده و `deploy.sh` دستور `tenants:refresh-webhook` را فقط به‌صورت کامنت دارد. یعنی از لحظه‌ی پایان deploy تا اجرای دستی آن دستور، بله روی مسیر قدیمی POST می‌کند و ۴۰۴ می‌گیرد — پیام‌های نمایندگان در آن بازه از بین می‌رود.

**کاری که باید بکنی:**

1. یک مسیر سازگاری موقت اضافه کن که به سازمان پیش‌فرض (کم‌ترین `id` از میان مستأجرهای فعال دارای توکن) وصل شود:

```php
// موقت — بعد از اجرای موفق tenants:refresh-webhook در deploy بعدی حذف شود
Route::post('/api/bot/webhook', function (Request $request, BotController $c) {
    $tenant = Tenant::whereNotNull('bot_token')
        ->where('status', Tenant::STATUS_ACTIVE)
        ->orderBy('id')->firstOrFail();
    return $c->handle($request, $tenant);
})->name('bot.webhook.legacy');
```

2. **الگوی استثنای CSRF را اصلاح کن.** الگوی فعلی `'api/bot/webhook/*'` مسیر بدونِ اسلش را پوشش نمی‌دهد و آن مسیر ۴۱۹ می‌گیرد:

```php
$middleware->preventRequestForgery(except: [
    'api/bot/webhook',
    'api/bot/webhook/*',
]);
```

3. در `deploy.sh` بعد از `migrate --force` یک هشدار پررنگ چاپ کن که تا اجرا نشدن `tenants:refresh-webhook` ربات‌ها روی مسیر قدیمی‌اند.

## الف-۲ 🔴 وابستگی `refresh-webhook` به `APP_URL`

`Tenant::webhookUrl()` از `route()` استفاده می‌کند و در محیط کنسول، لاراول آدرس را از `APP_URL` می‌سازد نه از هاست درخواست. مقادیر فعلی ناهماهنگ‌اند: `.env` لوکال `http://localhost:8000`، `.env.docker.example` مقدار `https://a1-studio.ir`، و در `CLAUDE.md` پنل روی `https://laravel-noejus.chbkn.run` است. اگر `APP_URL` سرور اشتباه باشد، `setWebhook` یک آدرس غیرقابل‌دسترس ثبت می‌کند، دستور «موفق» گزارش می‌دهد و ربات بی‌سروصدا می‌میرد.

**کاری که باید بکنی:**

- در ابتدای `RefreshTenantWebhook::handle()` مقدار `config('app.url')` را چاپ کن و اگر شامل `localhost` یا `127.0.0.1` بود یا با `https://` شروع نمی‌شد، **دستور را متوقف کن** و خطا بده.
- بعد از هر `setWebhook` موفق، همان URL ثبت‌شده را در خروجی چاپ کن (الان می‌کند — حفظش کن) و علاوه بر آن با فراخوانی `getWebhookInfo` از بله، آدرسی که واقعاً روی بله ثبت شده را هم نشان بده تا قابل مقایسه باشد.

## الف-۳ 🔴 throttle وبهوک بین همه‌ی مستأجرها مشترک است

`->middleware('throttle:120,1')` روی مسیر وبهوک، چون کاربر احرازشده‌ای وجود ندارد، کلید را از **IP** می‌سازد. همه‌ی درخواست‌های بله از چند IP محدود می‌آید، پس این سقفِ کل ربات‌های این نصب است نه هر مستأجر.

**کاری که باید بکنی:** یک rate limiter نام‌دار در `AppServiceProvider::boot()` تعریف کن که کلیدش مستأجر باشد، و روی مسیر از آن استفاده کن:

```php
RateLimiter::for('bot-webhook', fn (Request $request) =>
    Limit::perMinute(300)->by(
        optional($request->route('tenant'))->id ?? $request->ip()
    ));
```

## الف-۴ 🟠 ایمیل سازمان ردشده برای همیشه سوخته می‌شود

چون `users.email` سراسری unique است و رد کردن سازمان رکورد `User` را حذف نمی‌کند، آن شخص دیگر هرگز نمی‌تواند با همان ایمیل ثبت‌نام کند و پیام خطایی که می‌گیرد («این ایمیل قبلاً ثبت شده») گمراه‌کننده است.

**با حذف شدن گردش تایید دستی (بخش ب)، وضعیت `rejected` معنای قبلی‌اش را از دست می‌دهد**، ولی همین مشکل برای سازمان‌های `suspended` یا منقضی‌شده باقی است. مطمئن شو کاربری که سازمانش معلق/منقضی است، با همان ایمیل لاگین می‌کند و به صفحه‌ی تمدید می‌رود — نه اینکه مجبور به ثبت‌نام دوباره شود.

## الف-۵ 🟠 `migrate:rollback` داده‌ی همه‌ی مستأجرها را قاطی می‌کند

متد `down()` در `2026_08_25_000003_backfill_default_tenant.php` روی همه‌ی جدول‌ها `update(['tenant_id' => null])` می‌زند. اگر روزی روی production با چند مستأجر rollback بزنی، مالکیت همه‌ی رکوردها پاک می‌شود و بازگشتی ندارد.

**کاری که باید بکنی:** در ابتدای `down()` اگر تعداد رکوردهای `tenants` بیشتر از ۱ بود، صریحاً استثنا بده:

```php
if (DB::table('tenants')->count() > 1) {
    throw new RuntimeException(
        'rollback این migration با بیش از یک مستأجر داده را نابود می‌کند. اگر واقعاً می‌خواهی، دستی انجام بده.'
    );
}
```

## الف-۶ 🟠 نشت `TenantContext` در worker های ماندگار

`TenantContext` حالت static دارد و بین درخواست‌ها پاک نمی‌شود. با php-fpm و `QUEUE_CONNECTION=sync` امروز مشکلی نیست، ولی لحظه‌ای که Octane یا یک queue worker واقعی اضافه شود، مستأجر از یک job به job بعدی نشت می‌کند — یعنی داده‌ی سازمان A به سازمان B نشان داده می‌شود.

**کاری که باید بکنی:** در ابتدای `ResolveTenant::handle()` و ابتدای `BotController::handle()` یک `TenantContext::forget()` بزن (قبل از `set()`). بیمه‌ی ارزانی است.

## الف-۷ 🟠 `reject()` توکن ربات را برای همیشه پاک می‌کند

`TenantController::reject()` متد `disconnect()` را صدا می‌زند که `bot_token` را null می‌کند، در حالی که `suspend()` درست عمل می‌کند و فقط وبهوک را برمی‌دارد. در مدل جدید (بخش ب) هر جا سازمان **موقتاً** غیرفعال می‌شود (تعلیق، انقضای اشتراک)، فقط `deleteWebhook` بزن و **توکن را دست نزن** — تا با تمدید، ربات بدون دخالت کاربر برگردد.

## الف-۸ 🟡 seeder ها و ابزارهای import هنوز tenant-aware نیستند

`database/seeders/TreeFieldsSeeder.php` و `app/Console/Commands/ImportFromSqlite.php` با `DB::table(...)->insert()` رکورد می‌سازند بدون `tenant_id` — یعنی رکوردهایی با `tenant_id = null` که از دید کل اپ نامرئی‌اند. هر دو ابزار dev هستند، ولی سکوتشان خطرناک است.

**کاری که باید بکنی:** به هر دو یک آرگومان/آپشن `--tenant` اجباری اضافه کن و اگر داده نشد با خطا متوقف شو.

## الف-۹ 🟡 نوشتن بی‌صدای `tenant_id = null` داخل `withoutScope()`

در `BelongsToTenant::creating`، وقتی `TenantContext::bypassed()` باشد و `tenant_id` هم null، رکورد با `tenant_id = null` ساخته می‌شود بدون هیچ خطایی. این دقیقاً همان تله‌ی «رکورد نامرئی» است.

**کاری که باید بکنی:** داخل `withoutScope()` هم اگر `tenant_id` صریحاً ست نشده باشد، استثنا بده. `withoutScope` باید فقط برای **خواندن** بین‌مستأجری باشد، نه ساختن.

## الف-۱۰ 🟡 لاگ کامل payload وبهوک

`Log::info` کل آپدیت بله را ذخیره می‌کند که شامل شماره تلفن و متن پیام نمایندگان است. این از قبل بوده، ولی حالا که چند سازمان روی یک نصب‌اند، حجم و حساسیتش بیشتر است. حداقل در `APP_ENV=production` فقط `update_id`، نوع آپدیت و `tenant_id` را لاگ کن، نه کل بدنه.

---

# بخش ب — اشتراک، پرداخت و کد تخفیف

## ب-۱ تغییر مدل وضعیت مستأجر

گردش «تایید دستی» حذف می‌شود. وضعیت‌های جدید `Tenant::status`:

| وضعیت | معنی | لاگین پنل | ربات کار می‌کند؟ |
|---|---|---|---|
| `pending_payment` | ثبت‌نام کرده، هنوز پرداخت نکرده | ✅ فقط صفحه‌ی پرداخت | ❌ |
| `active` | اشتراک معتبر | ✅ کامل | ✅ |
| `expired` | اشتراک تمام شده | ✅ فقط صفحه‌ی تمدید | ❌ |
| `suspended` | سوپرادمین دستی معلق کرده | ❌ | ❌ |

**مهم:** برخلاف فاز ۱، کاربرِ پرداخت‌نکرده **باید بتواند لاگین کند** — وگرنه هیچ‌وقت به صفحه‌ی پرداخت نمی‌رسد. گیت باید داخل پنل باشد، نه روی خود لاگین.

ستون‌های جدید روی `tenants`:

```
subscription_ends_at   timestamp nullable   — null یعنی «هنوز اشتراکی ندارد» یا «نامحدود» (با ستون زیر تفکیک می‌شود)
is_unlimited           boolean default false — اشتراک بدون محدودیت زمانی (تصمیم سوپرادمین)
trial_ends_at          timestamp nullable   — فعلاً استفاده نمی‌شود، برای آینده
```

`status` را **محاسبه‌شده نگه ندار** — یک ستون واقعی باشد که با یک متد `Tenant::refreshStatus()` و یک کرون روزانه (`subscriptions:expire`) هم‌گام می‌شود. دلیل: کوئری‌های سوپرادمین و کرون‌ها باید بتوانند روی `status` ایندکس بزنند.

متد کلیدی:

```php
public function hasActiveSubscription(): bool
{
    if ($this->status === self::STATUS_SUSPENDED) return false;
    if ($this->is_unlimited) return true;
    return $this->subscription_ends_at !== null && $this->subscription_ends_at->isFuture();
}
```

و `botIsUsable()` باید حتماً این را هم چک کند — یعنی **با تمام شدن اشتراک، ربات آن مستأجر خودکار از کار می‌افتد**، نه فقط پنلش.

## ب-۲ جدول‌های جدید

### `payments`
```
id
tenant_id            FK
user_id              FK nullable        — چه کسی پرداخت را شروع کرد
amount               unsignedBigInteger — مبلغ نهایی پس از تخفیف (تومان)
original_amount      unsignedBigInteger — مبلغ قبل از تخفیف
discount_code_id     FK nullable
discount_amount      unsignedBigInteger default 0
days_granted         unsignedInteger    — چند روز بابت این پرداخت داده شد
gateway              string default 'zarinpal'
authority            string nullable unique  — شناسه‌ی تراکنش زرین‌پال
ref_id               string nullable    — کد رهگیری نهایی پس از verify
status               enum('pending','paid','failed','canceled') default 'pending'
paid_at              timestamp nullable
raw_response         json nullable      — پاسخ خام درگاه، برای پیگیری اختلاف
timestamps
```

### `discount_codes`
```
id
code                 string unique      — حروف بزرگ، بدون فاصله
percent              unsignedTinyInteger — ۱ تا ۱۰۰
max_uses             unsignedInteger nullable — null = نامحدود
used_count           unsignedInteger default 0
starts_at            timestamp nullable
expires_at           timestamp nullable
is_active            boolean default true
created_by           FK users nullable
timestamps
```

هر دو جدول **سراسری‌اند و `tenant_id` مستأجر ندارند** به این معنا که کد تخفیف را سوپرادمین می‌سازد و برای همه معتبر است؛ ولی `payments.tenant_id` مالکیت را مشخص می‌کند. مدل `Payment` را با `BelongsToTenant` بساز، مدل `DiscountCode` را **بدون** آن (چون مال پلتفرم است).

### تنظیمات پلتفرم
یک جدول کوچک `platform_settings` (کلید/مقدار، بدون `tenant_id`) یا یک فایل کانفیگ برای:
`price_per_day`، `min_payment_amount`، `currency` (تومان/ریال — دقت کن زرین‌پال بر حسب **ریال** کار می‌کند مگر پارامتر currency را ست کنی)، `zarinpal_merchant_id`، `zarinpal_sandbox` (bool).

**جدول `settings` موجود را برای این‌ها استفاده نکن** — آن حالا per-tenant است و مقادیر پلتفرم را قاطی می‌کند.

## ب-۳ اتصال زرین‌پال

یک سرویس `App\Services\Payment\ZarinpalGateway` بساز با دو متد: `request(Payment $payment): string` (برگرداندن URL هدایت) و `verify(Payment $payment): bool`.

نقطه‌ی شروع API (v4) — **حتماً قبل از پیاده‌سازی با مستندات رسمی فعلی زرین‌پال تطبیق بده، چون این API تغییر می‌کند و من نمی‌توانم نسخه‌ی امروزش را تضمین کنم:**

```
POST https://payment.zarinpal.com/pg/v4/payment/request.json
  { merchant_id, amount, callback_url, description, metadata: { mobile, email } }
  → data.authority

هدایت کاربر به: https://payment.zarinpal.com/pg/StartPay/{authority}

POST https://payment.zarinpal.com/pg/v4/payment/verify.json
  { merchant_id, amount, authority }
  → data.code == 100 (موفق) یا 101 (قبلاً verify شده)
```

برای تست، حالت sandbox زرین‌پال را پشت فلگ `zarinpal_sandbox` بگذار.

### 🔒 قواعد امنیتی پرداخت (هیچ‌کدام اختیاری نیست)

1. **مبلغ را هرگز از درخواست کاربر در مرحله‌ی verify نخوان.** مبلغ را موقع ساخت رکورد `Payment` محاسبه و ذخیره کن، و در `verify` همان مقدار ذخیره‌شده را به زرین‌پال بفرست. اگر مبلغِ برگشتی با مبلغ ذخیره‌شده نخواند، پرداخت را `failed` کن و لاگ هشدار بزن.
2. **صرفاً به `Status=OK` در callback اعتماد نکن.** فعال‌سازی اشتراک فقط و فقط بعد از پاسخ موفق `verify` سمت سرور انجام شود.
3. **callback را idempotent کن.** اگر کاربر URL بازگشت را دوباره باز کرد یا زرین‌پال دوبار صدا زد، نباید دو بار اشتراک اضافه شود. رکورد `Payment` را با `lockForUpdate()` بخوان و اگر `status === 'paid'` بود فقط رسید را نشان بده.
4. **`authority` را unique بگیر** تا همین تضمین در سطح دیتابیس هم باشد.
5. **مصرف کد تخفیف را اتمیک کن.** `used_count` را با `increment()` داخل همان تراکنشِ verify بالا ببر، نه موقع ساختن `Payment` — وگرنه کدی که پرداختش ناموفق بوده هم مصرف می‌شود. اعتبارسنجی `max_uses` را با یک `UPDATE ... WHERE used_count < max_uses` انجام بده تا race نداشته باشی.
6. **تخفیف را سمت سرور دوباره حساب کن.** کد تخفیفی که کاربر در فرم می‌زند فقط یک رشته است؛ درصد و اعتبارش را از دیتابیس بخوان.
7. مسیر callback را از CSRF مستثنا کن (اگر زرین‌پال با GET برمی‌گردد لازم نیست، ولی چک کن) و روی آن `throttle` بگذار.
8. `raw_response` را ذخیره کن ولی مطمئن شو `merchant_id` داخلش لاگ نمی‌شود.

## ب-۴ گردش کاربر

```
/register  →  ساخت tenant با status = pending_payment  →  لاگین خودکار
           →  redirect به /billing
/billing   →  نمایش وضعیت اشتراک + فرم پرداخت (مبلغ یا تعداد روز + فیلد کد تخفیف)
           →  «اعمال کد تخفیف» (AJAX، بدون ترک صفحه) → نمایش مبلغ نهایی و روز معادل
           →  «پرداخت» → ساخت Payment(pending) → هدایت به زرین‌پال
/billing/callback → verify → در صورت موفقیت:
           subscription_ends_at = max(now, subscription_ends_at) + days_granted
           status = active
           → صفحه‌ی رسید با کد رهگیری
/billing/invoices → تاریخچه‌ی پرداخت‌های همین سازمان
```

**نکته‌ی مهم درباره‌ی `ResolveTenant`:** الان هر کاربر غیرفعال را logout می‌کند. باید عوض شود به:

- `suspended` → logout با پیام (سوپرادمین عمداً بسته)
- `pending_payment` یا `expired` → **logout نکن**؛ اگر مقصد جزو مسیرهای `billing.*` یا `logout` نیست، به `/billing` هدایت کن.
- `active` → عبور عادی

## ب-۵ کد تخفیف در پنل سوپرادمین

CRUD کامل زیر `/platform/discount-codes`: ساخت، ویرایش، فعال/غیرفعال، حذف (فقط اگر استفاده نشده). ستون‌های لیست: کد، درصد، مصرف‌شده از سقف، بازه‌ی اعتبار، وضعیت.

اعتبارسنجی موقع اعمال: `is_active` و `starts_at <= now <= expires_at` و `max_uses === null || used_count < max_uses`. پیام خطای فارسی و مشخص برای هر حالت بده (منقضی / سقف پر شده / نامعتبر) — نه یک «کد نامعتبر» کلی.

## ب-۶ کرون انقضا

یک دستور `subscriptions:expire` که روزانه اجرا می‌شود:
- مستأجرهای `active` که `is_unlimited = false` و `subscription_ends_at < now` → `status = expired` و `deleteWebhook` روی ربات‌شان (توکن را **پاک نکن**).
- آن را در `routes/console.php` زمان‌بندی کن و در فایل `cron-jobs` پروژه هم اضافه کن.

---

# بخش ج — پنل سوپرادمین: فقط نظارت

سوپرادمین پلتفرم **مالک هیچ رباتی نیست** و نباید بتواند ربات اضافه کند، توکن وارد کند، یا داده‌ی کسی را ویرایش کند. کارش سه چیز است: دیدن، مدیریت اشتراک، تعلیق.

## ج-۱ چیزهایی که باید بتواند

**داشبورد پلتفرم** — تعداد کل سازمان‌ها به تفکیک وضعیت، تعداد سازمان‌های دارای ربات متصل، مجموع درآمد (کل / ماه جاری)، لیست پرداخت‌های اخیر، سازمان‌هایی که اشتراکشان تا ۷ روز آینده تمام می‌شود.

**لیست سازمان‌ها** — با فیلتر وضعیت و جستجو روی نام/ایمیل، و ستون‌های: نام، مالک، وضعیت، تاریخ پایان اشتراک، آیا ربات متصل است، تعداد نماینده، تعداد گزارش.

**صفحه‌ی جزئیات یک سازمان** — این مهم‌ترین بخش است. باید بتواند **داده‌های همان سازمان را به‌صورت مجزا رصد کند**: نمایندگان، دپارتمان‌ها، دسته‌بندی‌ها و فرم‌ها، گزارش‌های ثبت‌شده (با امکان باز کردن یک گزارش)، پیام‌های همگانی، تاریخچه‌ی پرداخت‌ها، و وضعیت اتصال ربات (فقط نام کاربری ربات و تاریخ اتصال — **نه خود توکن**).

**مدیریت اشتراک هر سازمان** — تعیین دستی `subscription_ends_at`، دکمه‌های میان‌بر (+۷ روز، +۱ ماه، +۳ ماه، +۱ سال)، تیک «نامحدود»، و امکان کوتاه کردن دوره. هر تغییر دستی باید در یک جدول `subscription_logs` ثبت شود (چه کسی، چه زمانی، از چه تاریخی به چه تاریخی، با چه توضیحی).

**تعلیق و رفع تعلیق** — همان رفتار فاز ۱، با اصلاح الف-۷ (توکن پاک نشود).

**کد تخفیف** — بخش ب-۵.

## ج-۲ چیزهایی که نباید بتواند

- ورود به `/admin` (پنل مستأجر). میدلور `ResolveTenant` الان درست عمل می‌کند و او را به `/platform` برمی‌گرداند — حفظش کن.
- وارد کردن یا دیدن `bot_token` هیچ مستأجری. در `Tenant` مقدار `bot_token` در `$hidden` هست ولی این فقط سریال‌سازی را می‌پوشاند؛ مطمئن شو در هیچ ویوی `platform.*` مستقیم چاپ نمی‌شود.
- `setWebhook` زدن یا ربات وصل کردن برای کسی. `BotConnector::setWebhook` نباید از هیچ کنترلر `Platform` صدا زده شود — فقط `deleteWebhook` (برای تعلیق/انقضا) مجاز است.
- ویرایش یا حذف داده‌ی کسب‌وکاری مستأجرها (گزارش، نماینده، دسته‌بندی). همه‌ی صفحات نظارتی **فقط‌خواندنی** باشند.

## ج-۳ چطور داده‌ی مستأجر را بخواند بدون شکستن ایزولاسیون

این نکته‌ی فنی مهم فاز ۲ است. global scope فعلی سخت‌گیرانه است و بدون مستأجر هیچ رکوردی برنمی‌گرداند — که درست است و **نباید سستش کنی**. برای صفحات نظارتی از `TenantContext::forTenant()` استفاده کن، نه از `withoutScope()`:

```php
$data = TenantContext::forTenant($tenant, fn () => [
    'reports' => Report::with('representative', 'category')->latest()->paginate(20),
    'reps'    => Representative::with('province')->get(),
]);
```

سه قاعده:

1. **هرگز `withoutScope()` را در کنترلرهای `Platform` استفاده نکن** — چون هر اشتباه کوچکی در آن حالت یعنی قاطی شدن داده‌ی همه‌ی سازمان‌ها در یک صفحه.
2. یک میدلور یا trait مشترک بساز که تضمین کند درخواست‌های `platform.*` هیچ‌وقت `TenantContext` را برای کل طول درخواست ست نمی‌کنند — فقط داخل closure های `forTenant`.
3. برای هر صفحه‌ی نظارتی یک تست بنویس که تأیید کند فقط رکوردهای همان سازمان برمی‌گردد.

## ج-۴ تفکیک بصری

پنل پلتفرم باید فوراً از پنل مستأجر قابل تشخیص باشد: layout جدا (`resources/views/layouts/platform.blade.php`)، رنگ header متفاوت، و یک نوار بالای صفحه با متن «پنل نظارت پلتفرم». هیچ آیتم منوی مشترکی با پنل مستأجر نداشته باشد.

---

## ۴. مهاجرت داده

سازمان پیش‌فرض («حوزه هنری») که در فاز ۱ ساخته می‌شود، در فاز ۲ باید:

- `status = 'active'`
- `is_unlimited = true` (این سازمان اصلی است و نباید بابت اشتراک قطع شود)

چون فاز ۱ هنوز deploy نشده، **بهتر است migration فاز ۱ را دست نزنی و یک migration جدید بنویسی** که وضعیت‌های قدیمی را نگاشت کند:

```
approved → active  (و برای سازمانِ id کمینه: is_unlimited = true)
pending  → pending_payment
rejected → suspended
```

اگر ثابت شد فاز ۱ اصلاً روی هیچ محیطی اجرا نشده، می‌توانی به‌جایش migration های فاز ۱ را مستقیم اصلاح کنی — این را از من بپرس، خودت تصمیم نگیر.

---

## ۵. چک‌لیست تست

تست‌های `MultiTenancyTest` موجود باید همچنان سبز بمانند (بعضی‌شان با تغییر وضعیت‌ها نیاز به به‌روزرسانی دارند). این‌ها را اضافه کن:

1. ثبت‌نام → لاگین موفق → هدایت اجباری به `/billing`، و عدم دسترسی به `/admin/reports`.
2. پرداخت موفق (با mock کردن `Http::fake()` روی زرین‌پال) → `status = active`، `subscription_ends_at` درست محاسبه شده، ربات قابل استفاده.
3. **دوباره صدا زدن callback با همان `authority`** → اشتراک دوبار اضافه نشود.
4. **دستکاری مبلغ**: رکورد `Payment` با مبلغ X ساخته شود ولی درگاه مبلغ Y برگرداند → پرداخت `failed` شود و اشتراک فعال نشود.
5. کد تخفیف ۲۰٪ → مبلغ نهایی درست، و `used_count` فقط بعد از پرداخت **موفق** یک واحد بالا برود.
6. کد تخفیف با `max_uses` پرشده / منقضی / غیرفعال → رد شود با پیام مشخص.
7. انقضای اشتراک (`subscriptions:expire`) → `status = expired`، وبهوک حذف شود، **`bot_token` دست‌نخورده بماند**، و وبهوک ورودی آن مستأجر دیگر پردازش نشود.
8. تمدید بعد از انقضا → ربات بدون وارد کردن دوباره‌ی توکن برگردد.
9. سوپرادمین: صفحه‌ی جزئیات سازمان A فقط گزارش‌های A را نشان دهد و هیچ رکوردی از B در آن نباشد.
10. سوپرادمین نتواند به `/admin/*` برود و هیچ مسیری برای `setWebhook` نداشته باشد.
11. کاربر مستأجر نتواند به `/platform/*` برود (۴۰۳ یا redirect).
12. تعیین دستی دوره توسط سوپرادمین در `subscription_logs` ثبت شود.
13. همه‌ی موارد بخش الف: مخصوصاً مسیر legacy وبهوک (۲۰۰ بگیرد نه ۴۱۹) و اینکه `refresh-webhook` با `APP_URL` نامعتبر متوقف شود.

---

## ۶. ترتیب پیشنهادی اجرا

1. **بخش الف** (رفع ایرادها) — کامل، با تست.
2. schema فاز ۲: `payments`, `discount_codes`, `platform_settings`, `subscription_logs`, ستون‌های اشتراک روی `tenants` + migration نگاشت وضعیت.
3. مدل‌ها، `hasActiveSubscription()`، اصلاح `botIsUsable()` و `ResolveTenant`.
4. سرویس زرین‌پال + صفحه‌ی `/billing` + callback + رسید.
5. کد تخفیف (مدل، اعتبارسنجی، CRUD سوپرادمین).
6. بازطراحی پنل پلتفرم: داشبورد، صفحات نظارتی فقط‌خواندنی، مدیریت اشتراک، layout جدا.
7. کرون `subscriptions:expire`.
8. تست‌ها و مستندسازی در `CLAUDE.md` / `AGENTS.md`.

بعد از هر مرحله بگو با چه دستوری تأییدش کنم. **دستورات production را فقط بنویس، خودت اجرا نکن.** برای درگاه، تا وقتی `merchant_id` واقعی را نداده‌ام فقط حالت sandbox را راه بینداز.

---

## ۷. سوالاتی که باید بپرسی

- تفسیر «پرداخت آزاد + دسترسی فوری» در بخش ۰ را تأیید می‌کنم یا مدل دیگری می‌خواهم؟
- `price_per_day` و حداقل مبلغ اولیه چقدر باشد؟
- زرین‌پال بر حسب ریال کار می‌کند؛ نمایش قیمت‌ها در پنل به تومان باشد یا ریال؟
- سازمان اصلی («حوزه هنری») واقعاً باید نامحدود و معاف از پرداخت باشد؟
- آیا فاکتور/رسید رسمی (با شماره فاکتور و اطلاعات هویتی) لازم است یا همان کد رهگیری کافی است؟
- بعد از انقضا، داده‌های سازمان تا چه مدت نگه داشته شود؟ (پیشنهاد: نامحدود نگه داشته شود، فقط دسترسی قطع شود.)
